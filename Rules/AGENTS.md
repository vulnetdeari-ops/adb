# Factory path

Canonical agent rules: [`../AGENTS.md`](../AGENTS.md) at the repository root. Setup copies **that** file into each app. Edit the root file, not this one.

This path exists so older factory checks still find `Rules/AGENTS.md`.
