---
name: adb-status
description: "Rewrite ADB STATUS and judge the work against the Completion Standard."
---

# /adb-status

Canonical: `/Users/bubby/Development/_System/Methods/ADB/SKILL.md`. Ignore a leftover project `ADB.md`.
Reload the Source of Truth files that exist. Those files outrank chat.

1. **Rewrite** `adb/07-STATUS.md` header using the field names in SKILL.md (07). Concise overwrite, not a changelog. Preserve `## Open issues` (when collapsed), `## Execution plan`, and `## Readiness` verbatim. Do not invent a READINESS key — only `/adb-ready` writes it.
2. **Layout** — numbered `adb/` names only; one issue register (08, or STATUS while collapsed — not both). Leftover project `ADB.md` is not the method.
3. **Drift** — code vs SoT (SKILL.md 55–57). Accidental code is not new product truth.
4. **CARRIED** — this review counts (SKILL.md 50A).
5. **Completion** — SKILL.md 61. Any no → not complete. Stamp missing or stale → next is `/adb-ready`. ALPHA is never completion.
6. **Optional** — method defect → append `LESSONS.md` in the ADB repo (SKILL.md 61A). Still fix the project.

Return: phase, what closed, blockers, next action, issue counts.
